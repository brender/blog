<%= form_for([@post, @post.comments.build]) do |f| %>
<div class="write_comment">
  <div class="field">
    <%= f.text_field :commenter, :placeholder => "Name", class: "text_input" %>
  </div>
  <div class="field">
    <%= f.text_area :body, :placeholder => "Comment", class: "text_input" %>
  </div>
  <div class="submit">
    <%= f.submit %>
  </div>
</div>
<% end %>